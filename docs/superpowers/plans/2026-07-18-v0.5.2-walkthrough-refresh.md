# v0.5.2 Walkthrough Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the website clearly distinguish the bounded `quickstart` proof from the complete matrix and replace the old walkthrough with evidence from the public v0.5.2 command.

**Architecture:** Keep the existing homepage and Markdown-backed docs structure. Tighten the hero and walkthrough copy around two explicit workflows, then replace the two bundled PNG assets with captures derived from one real `psbt-interop-lab@0.5.2 quickstart` run and its generated HTML report.

**Tech Stack:** React 19, TypeScript, Vitest, Vite, Playwright CLI, PSBT Interop Lab CLI v0.5.2.

## Global Constraints

- Preserve the approved homepage layout, dark/light themes, and orange accent system.
- Call `quickstart` a bounded first-run proof, not the complete interoperability matrix.
- Call `matrix` the complete 31-scenario suite.
- Use only output produced by the public `psbt-interop-lab@0.5.2` package.
- Keep the screenshots at 1440 x 900 and readable on the existing responsive cards.

---

### Task 1: Lock The Workflow Messaging

**Files:**
- Modify: `website/src/App.test.tsx`
- Modify: `test/release-documentation.test.ts`

**Interfaces:**
- Consumes: Existing homepage and README copy.
- Produces: Assertions requiring a first-run quickstart CTA, bounded handoff language, and a separate complete-matrix description.

- [x] **Step 1: Add failing tests for the quickstart and matrix distinction**

Require the homepage to expose a `Start quickstart` link to `/docs#quick-start`, identify the walkthrough as v0.5.2 evidence, and describe the full matrix separately.

- [x] **Step 2: Run focused tests and confirm they fail for the old wording**

Run: `pnpm --dir website test -- --run src/App.test.tsx && pnpm test -- test/release-documentation.test.ts`

Expected: The new copy and CTA assertions fail before implementation.

### Task 2: Align Homepage, Website Docs, And README

**Files:**
- Modify: `website/src/components/Hero.tsx`
- Modify: `website/src/components/ProofWalkthrough.tsx`
- Modify: `website/src/content.ts`
- Modify: `README.md`

**Interfaces:**
- Consumes: The wording contract from Task 1.
- Produces: A consistent explanation of `quickstart` and `matrix` across the homepage, website docs, GitHub README, and future npm package README.

- [x] **Step 1: Implement the minimal copy and CTA changes**

The hero CTA must open the Quick Start documentation. The walkthrough must show the v0.5.2 command proving environment checks, five detector canaries, one Core-to-Rust-to-Core handoff, policy acceptance, report creation, and automatic cleanup. The matrix remains explicitly described as the complete 31-scenario run.

- [x] **Step 2: Run focused tests and confirm they pass**

Run: `pnpm --dir website test -- --run src/App.test.tsx && pnpm test -- test/release-documentation.test.ts`

Expected: Both focused suites pass.

### Task 3: Capture Real v0.5.2 Evidence

**Files:**
- Modify: `docs/assets/walkthrough/cli-finding-and-replay.png`
- Modify: `docs/assets/walkthrough/compatibility-report.png`

**Interfaces:**
- Consumes: Public `npx --yes psbt-interop-lab@0.5.2 quickstart` output and its generated `report.html`.
- Produces: Two 1440 x 900 PNG captures bundled by both README and website.

- [x] **Step 1: Run the public quickstart command against a fresh artifact directory**

Run: `npx --yes psbt-interop-lab@0.5.2 quickstart --no-build --artifacts /private/tmp/psbt-v052-walkthrough`

Expected: Five detector canaries pass; `happy-path` passes Core policy; an artifact directory is printed; Core is stopped automatically.

- [x] **Step 2: Capture the actual CLI transcript and generated report**

Render the captured transcript without changing semantic output, shortening only the absolute temporary artifact path. Capture the generated self-contained HTML report directly. Save both at 1440 x 900 over the existing asset paths.

- [x] **Step 3: Inspect both PNGs for legibility and truthful v0.5.2 content**

Expected: The command, checks, handoff, policy result, artifact location, and cleanup are legible; the report shows the same run ID and `happy-path` result.

### Task 4: Verify And Ship The Website Change

**Files:**
- Verify: `website/src/styles.css`
- Verify: `website/src/routes.test.tsx`

**Interfaces:**
- Consumes: Updated copy and assets.
- Produces: A tested production website with no regression to docs routes or responsive layouts.

- [x] **Step 1: Run full project and website verification**

Run: `pnpm test && pnpm typecheck && pnpm --dir website test -- --run && pnpm --dir website lint && pnpm --dir website build`

Expected: All commands exit successfully.

- [x] **Step 2: Inspect desktop and mobile website renders**

Capture the homepage at 1440 x 1024 and 390 x 844. Confirm no overflow, overlap, stale v0.5.1 labels, broken images, or console errors.

- [x] **Step 3: Review the diff and commit**

Run: `git diff --check && git diff --stat`

Expected: No whitespace errors and only scoped messaging, test, plan, and walkthrough-asset changes.
